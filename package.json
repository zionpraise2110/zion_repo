{
  "name": "bible-chat-app",
  "version": "1.0.0",
  "description": "AI-powered Bible chat application with authentication",
  "type": "module",
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "openai": "^4.20.1",
    "axios": "^1.6.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
